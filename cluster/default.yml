default-master:
  provider: vbox_wifi
  memory: 4096
  cpus: 1
  minion:
    master: default-master
  grains:
    roles:
      - postgresql
      - zookeeper.server
      - mesos.master
      - mesos.slave
  synced_folders:
    ./states: /srv/states
    ./pillar: /srv/pillar

